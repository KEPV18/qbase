// Stub: QMS Personal uses Google Sheets; QBase uses Supabase
// This hook is NOT used in QBase — imported only for type compatibility in copied pages

export function useQMSData() {
  return { data: undefined, isLoading: false, error: null };
}