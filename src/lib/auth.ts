// Stub: Google auth not available in QBase (Supabase auth instead)
// This function is NOT used in QBase — imported only for type compatibility

export async function getAccessToken(): Promise<string | null> {
  return null;
}